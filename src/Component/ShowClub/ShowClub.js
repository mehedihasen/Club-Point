import React from 'react';

const ShowClub = (club) => {
    const leg = club.slice(0,15);
    console.log(leg);
    return (
        <div>
            
        </div>
    );
};

export default ShowClub;